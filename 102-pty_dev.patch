--- a/pty_dev.c	2020-10-13 13:20:41.792068909 +0300
+++ b/pty_dev.c	2020-10-13 13:17:11.935840261 +0300
@@ -42,8 +42,8 @@
 #if defined (HAVE_GETPT) && defined (HAVE_GRANTPT) && defined (HAVE_UNLOCKPT) && defined (HAVE_PTSNAME)
     char *ptyname;
 
-    if((mr_fd=getpt()) < 0)
- 	return -1;
+//    if((mr_fd=getpt()) < 0)
+// 	return -1;
     if(grantpt(mr_fd) != 0)
 	return -1;
     if(unlockpt(mr_fd) != 0)
